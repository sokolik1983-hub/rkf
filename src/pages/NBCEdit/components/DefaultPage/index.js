import React from 'react';
import Card from '../../../../components/Card';

const DefaultPage = () => {
    return (
        <Card>
            Не найдено.
        </Card>
    );
};

export default DefaultPage;
